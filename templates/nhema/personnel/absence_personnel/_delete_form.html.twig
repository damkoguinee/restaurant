<form method="post" action="{{ path('app_nhema_personnel_absence_personnels_delete', {'id': absence_personnel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ absence_personnel.id) }}">
    <button class="btn">Delete</button>
</form>
