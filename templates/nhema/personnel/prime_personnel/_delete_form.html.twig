<form method="post" action="{{ path('app_nhema_personnel_prime_personnel_delete', {'id': prime_personnel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prime_personnel.id) }}">
    <button class="btn">Delete</button>
</form>
