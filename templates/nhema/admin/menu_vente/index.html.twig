{% extends 'base.html.twig' %}

{% block title %}MenuVente index{% endblock %}

{% block body %}
    <h1>MenuVente index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for menu_vente in menu_ventes %}
            <tr>
                <td>{{ menu_vente.id }}</td>
                <td>{{ menu_vente.nom }}</td>
                <td>
                    <a href="{{ path('app_nhema_admin_menu_vente_show', {'id': menu_vente.id}) }}">show</a>
                    <a href="{{ path('app_nhema_admin_menu_vente_edit', {'id': menu_vente.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_nhema_admin_menu_vente_new') }}">Create new</a>
{% endblock %}
