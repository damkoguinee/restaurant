<form method="post" action="{{ path('app_nhema_admin_liaison_product_delete', {'id': liaison_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liaison_product.id) }}">
    <button class="btn">Delete</button>
</form>
