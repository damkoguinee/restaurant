{% extends 'base.html.twig' %}

{% block title %}LiaisonProduct{% endblock %}

{% block body %}
    <h1>LiaisonProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ liaison_product.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nhema_admin_liaison_product_index') }}">back to list</a>

    <a href="{{ path('app_nhema_admin_liaison_product_edit', {'id': liaison_product.id}) }}">edit</a>

    {{ include('nhema/admin/liaison_product/_delete_form.html.twig') }}
{% endblock %}
