<form method="post" action="{{ path('app_nhema_admin_type_plat_delete', {'id': type_plat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_plat.id) }}">
    <button class="btn">Delete</button>
</form>
