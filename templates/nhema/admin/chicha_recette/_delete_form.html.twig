<form method="post" action="{{ path('app_nhema_admin_chicha_recette_delete', {'id': chicha_recette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chicha_recette.id) }}">
    <button class="btn">Delete</button>
</form>
