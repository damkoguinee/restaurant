{% extends 'base.html.twig' %}

{% block title %}Dessert{% endblock %}

{% block body %}
    <h1>Dessert</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dessert.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ dessert.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ dessert.description }}</td>
            </tr>
            <tr>
                <th>PrixVente</th>
                <td>{{ dessert.prixVente }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nhema_admin_dessert_index') }}">back to list</a>

    <a href="{{ path('app_nhema_admin_dessert_edit', {'id': dessert.id}) }}">edit</a>

    {{ include('nhema/admin/dessert/_delete_form.html.twig') }}
{% endblock %}
