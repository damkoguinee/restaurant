{% extends 'base.html.twig' %}

{% block title %}Dessert index{% endblock %}

{% block body %}
    <h1>Dessert index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>PrixVente</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dessert in desserts %}
            <tr>
                <td>{{ dessert.id }}</td>
                <td>{{ dessert.nom }}</td>
                <td>{{ dessert.description }}</td>
                <td>{{ dessert.prixVente }}</td>
                <td>
                    <a href="{{ path('app_nhema_admin_dessert_show', {'id': dessert.id}) }}">show</a>
                    <a href="{{ path('app_nhema_admin_dessert_edit', {'id': dessert.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_nhema_admin_dessert_new') }}">Create new</a>
{% endblock %}
