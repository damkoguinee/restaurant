{{ form_errors(form) }}
{{ form_start(form) }}
    <div class="row mb-1"> 
        <div class="col-sm-12 col-md-2">
            <label class="form-label mx-1" for="">Type de entree* <a href="{{path('app_nhema_admin_type_entree_new') }}" class="btn btn-sm btn-warning"><i class="fas fa-plus"></i></a></label>
            <div class="mx-1">{{ form_widget(form.typeentree) }}</div>
        </div>
        <div class="col-sm-12 col-md-4">
            {{ form_row(form.nom) }}
        </div>

        <div class="col-sm-12 col-md-6">
            {{ form_row(form.description) }}
        </div>  
    </div>
    <div class="row mb-1"> 
        <div class="col-sm-12 col-md-4">
            {{ form_row(form.prixVente) }}
        </div>
        <div class="col-sm-12 col-md-2">
            {{ form_row(form.etat) }}
        </div>
        <div class="col-sm-12 col-md-4">
            {{ form_row(form.service) }}
        </div>    
    </div>
    <div class="row mb-1"> 
        <div class="col-sm-12 col-md-4">
            {{ form_row(form.image) }}
        </div>    
    </div>
    <button class="btn btn-primary">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
