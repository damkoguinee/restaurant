<form method="post" action="{{ path('app_nhema_vente_modification_commande_delete', {'id': modification_commande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ modification_commande.id) }}">
    <button class="btn">Delete</button>
</form>
