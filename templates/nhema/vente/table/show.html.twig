{% extends 'base.html.twig' %}

{% block title %}Table{% endblock %}

{% block body %}
    <h1>Table</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ table.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ table.nom }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ table.capacite }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ table.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nhema_vente_table_index') }}">back to list</a>

    <a href="{{ path('app_nhema_vente_table_edit', {'id': table.id}) }}">edit</a>

    {{ include('nhema/vente/table/_delete_form.html.twig') }}
{% endblock %}
